This page's title should be: "{{ page.title }}".

Now here's a link: {{ site.root }}
And using PieCrust's extension: {{ pcurl('about') }}
Tag URL? {{ pctagurl('some-tag') }}
Category URL? {{ pccaturl('some-category') }}
Post URL? {{ pcposturl('2011', '09', '01', 'some-post') }}
